import { NgNotIfDirective } from './ng-not-if.directive';

describe('NgNotIfDirective', () => {
  it('should create an instance', () => {
    const directive = new NgNotIfDirective();
    expect(directive).toBeTruthy();
  });
});
